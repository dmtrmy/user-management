// Redirect to the submission form when the button is clicked
document.getElementById('backToForm').addEventListener('click', () => {
    window.location.href = '/user'; // Adjust the URL if necessary
});